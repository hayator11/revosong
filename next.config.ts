import type { NextConfig } from "next";

// Force rebuild trigger: v3 - Copyright warnings, Information page, Menu updates
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
